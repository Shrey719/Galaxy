let config = {
    cloaking: true,
    ads: false, // ig if ppl want to watch ads they can? ALWAYS disabled by default
    lock: false, // Password system if someone wants to self host or smth idk
    winLock: false, // If os is windows, stop working 
    winCloak: false // If os is windows, cloak
}
 
